# 上下文摘要调优

## 当前边界

摘要结构靠五个固定标题字面校验，连续越预算时每轮都重新生成累计摘要，摘要输入预算取消息预算的一半。已有规则裁剪兜底，正确性不受影响，但摘要成本和质量未经真实长任务校准。

## 何时处理

先在真实多轮上下文验证中记录摘要失败率、每轮压缩次数、压缩前后 Token 和摘要请求成本；任一指标持续异常后再立项。

## 唯一方向

每次只改一处，优先把摘要输出改为 JSON 并严格解析；不同时引入摘要缓存、动态预算或 LLM judge。

## 非目标

- 不在有观测数据前调整压缩阈值或摘要预算
- 不删除或修改原始 JSONL 历史
