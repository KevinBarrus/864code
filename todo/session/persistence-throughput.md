# Session 持久化吞吐

## 当前边界

每条消息在追加到 JSONL 后立即 `fsync`，保证崩溃时单条消息不会丢失。这是有意选择的耐久性优先取舍，不是缺陷。

## 何时处理

先在代表性会话上实测；满足任一条件后再立项：

- 单个 Turn 的持久化总耗时超过 200 ms
- 单进程消息写入速率超过 50 条/秒

## 唯一方向

引入带最大等待时间的 group-commit：把多条消息合并为一次 `fsync`，用最大等待时间约束崩溃窗口。

## 非目标

- 不在满足触发条件前提前优化
- 不引入数据库或独立写入服务
- 不牺牲"按产生顺序追加"的语义
