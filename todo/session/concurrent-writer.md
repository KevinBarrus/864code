# Session 多进程写入

## 当前边界

v1 是单用户 TUI 加单进程模型，同一 Session 只有一个写入者。当前不提供跨进程文件锁，多个进程同时打开同一 Session 会让 JSONL 追加交错损坏。

## 何时处理

满足任一条件后再立项：

- 引入后台 Agent 或守护进程
- 支持多端同时恢复并写入同一 Session

## 唯一方向

采用"每个 Session 单写入者"的进程内队列或独立服务，所有写入经过该写入者；不让多个进程直接竞争 JSONL 文件锁。

## 非目标

- 不在单进程场景下引入文件锁
- 不引入数据库或分布式锁
