# TUI 富文本渲染与工具详情

## 当前边界

对话区是纯文本自然堆叠，不渲染 Markdown 或代码高亮；工具活动压缩为单行摘要，无法展开查看完整输出。键盘翻页（PageUp/PageDown）已经支持。

## 何时处理

确定终端渲染方案后再立项。

## 唯一方向

先确定 prompt-toolkit 下的代码块、列表、加粗渲染方案，再改对话条目模型；工具详情展开独立评估，不与富文本渲染耦合。

## 非目标

- 不在确定渲染方案前改动 ConversationEntry 模型
- 不引入 Web 前端或复杂文本引擎
