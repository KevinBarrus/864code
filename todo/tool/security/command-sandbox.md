# 命令工具沙箱

## 当前边界

`run_command` 使用 shell 执行命令，只有逐次人工审批、完整命令展示、工作区 cwd、超时和进程组回收。它不是文件、网络、CPU、内存或资源沙箱。

## 何时处理

当命令执行的真实风险超过人工审批可接受范围时立项。

## 唯一方向

选择外部隔离运行时（容器或沙箱进程），显式配置可写目录、网络和资源配额；不在 Python 层实现命令 allowlist/denylist 来伪装沙箱。

## 非目标

- 不用命令黑名单宣称完成沙箱隔离
- 不在单用户本地场景下引入重隔离
