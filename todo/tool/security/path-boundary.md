# 工具路径与命令边界

## 当前问题

文件工具限制在工作区内，但 `run_command` 使用 shell 执行命令，命令可能访问工作区之外的文件

## 待完成事项

- 明确文件工具和命令工具的不同访问边界
- 设计工作区外路径的用户授权流程
- 评估命令工具是否需要独立的审批范围
- 增加命令访问工作区外路径的测试
- 评估操作系统级沙箱或受限子进程方案
- 记录网络访问、环境变量和子进程权限边界

## 当前约束

- 文件工具默认拒绝工作区外路径
- `run_command` 当前只保证工作目录为工作区
- 第一版不实现完整操作系统级沙箱
