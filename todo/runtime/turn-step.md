# Turn / Step 持久化

## 当前边界

v1 中 Turn 是一次 `AgentLoop.run()`，Step 是其中一次模型请求。两者只存在于当前进程调用栈，不单独持久化或恢复

## 何时处理

满足任一条件后再立项：

- 需要从中断位置继续执行同一 Turn
- 需要并发执行子任务
- 需要按单次模型请求审计、计费或重放

## 唯一方向

新增可持久化的 Turn 与 Step 记录，由 Session 保存状态边界和顺序。不要先创建仅包装 AgentLoop 局部变量的空类
